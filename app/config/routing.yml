rest:
    type: annotation
    prefix: /api/{firewall}
    defaults: { _format: 'json'}
    resource: '../../src/Controller/'

dh_auditor:
    resource: "@DHAuditorBundle/Controller/"
    type: auditor
