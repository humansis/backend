<!DOCTYPE html>
<html>
    <head>
        <style>{{ source('@Common/Pdf/main.css') }}</style>
        <style>
            body {
                font-family: {{ font }};
            }

            header, footer, header h1 {
              font-weight: normal;
              color: {{ primaryColor }}
            }

            .beneficiary-name, .voucher-value {
              color : {{ secondaryColor }}
            }

            .address p {
                font-style: italic;
                margin-left: 30px;
            }

            .vendor-info, .beneficiary-info {
              margin-left: 30px;
            }

            .name, .value {
              font-weight:bold
            }

            h3 {
                margin-left: 5px;
                font-weight: normal;
                color:  {{ secondaryColor }}
            }

            .qr-code {
                height: 215px;
                position: relative;
                z-index: -1;
            }

            .qr-code img {
                margin-top: 120px;
                margin-left: 50%;
                position: relative;
                z-index: -1;
                transform: translate(-50%, -50%);
            }

            .explanation {
                background: white;
                z-index: 1;
                position: relative;
            }

            hr.separation {
                border: 1px dashed black;
                margin-top: -8px;
            }
            img.scissors {
                background: white;
                height: 14px;
            }
        </style>
    </head>
    <body>
       {% set vars = {'name': organizationName, 'logo': organizationLogo, 'footer': footer, 'primaryColor': primaryColor} %}
        {% include('@Common/Pdf/header.html.twig') with vars %}

        <main>
            <h3>Beneficiary's information</h3>
            <div class="beneficiary-info">
              <p class="name">
                <span>Beneficiary :</span>
                <span class="beneficiary-name"> Mr/Mrs {{ name }}</span>
              </p>
              <p class="value">
                <span>Total value of the booklet : </span>
                <span class="voucher-value">{{ value }}{{ currency }}</span>
              </p>
              <p class="explanation"> This booklet contains {{ numberVouchers }} vouchers. You can use them separately to buy products in a shop. <br>
                  Be aware though, that you cannot use the total amount of this booklet by scanning this code. It is an identification code, not a payment code.</p>
              <div class="qr-code">
                <img src="{{ qrCodeLink }}" />
              </div>
            </div>
            <h3>Vendor's information</h3>
            <div class="vendor-info">
            </div>
            <p class="next-voucher"></p>
        </main>
    </body>
</html>