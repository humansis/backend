files:
    "/etc/cron.d/mycron":
        mode: "000644"
        owner: webapp
        group: webapp
        content: |
            * * * * * /var/app/current /usr/local/bin/myscript.sh
    "/usr/local/bin/myscript.sh":
        mode: "000755"
        owner: webapp
        group: webapp
        content: |
            #!/bin/bash
            date > /tmp/date
            php bin/console reporting:data-indicator:add >> /var/log/cron
            exit 0
            
commands:
    01_remove_old_cron:
        command: "rm -f /etc/cron.d/*.bak"
