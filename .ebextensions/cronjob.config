files:
    "/etc/cron.d/mycron":
        mode: "000644"
        owner: root
        group: root
        content: |
            * * * * * root cd /var/app/ondeck && php bin/console reporting:data-indicator:add
            
commands:
    01_remove_old_cron:
        command: "rm -f /etc/cron.d/*.bak"
